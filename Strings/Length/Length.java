package com.kodnest.Strings.Length;

public class Length {
	boolean lengthFind(String s1,String s2)
	{
		if(s1.length()==s2.length())
		{
			return true;
		}
		else
		{
			return false;
		}
	}
}
