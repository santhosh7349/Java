LengthChecker:

Write a java program to take 2 strings as user input and checks whether they are of same length or not. If the strings are of same length then print “Both strings are of same length” otherwise prints “Both strings are of different length”.